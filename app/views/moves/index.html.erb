<p id="notice"><%= notice %></p>

<h1>Listing Moves</h1>

<table>
  <thead>
    <tr>
      <th>Username</th>
      <th>Friend username</th>
      <th>Board</th>
      <th>Direction</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @moves.each do |move| %>
      <tr>
        <td><%= move.username %></td>
        <td><%= move.friend_username %></td>
        <td><%= move.board_id %></td>
        <td><%= move.direction %></td>
        <td><%= link_to 'Show', move %></td>
        <td><%= link_to 'Edit', edit_move_path(move) %></td>
        <td><%= link_to 'Destroy', move, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Move', new_move_path %>
